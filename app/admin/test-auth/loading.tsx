import { PageLoadingTemplate } from '@/components/templates/page-template';

export default function TestAuthLoading() {
  return (
    <PageLoadingTemplate 
      title="🔍 Test Authentication" 
      description="Đang tải trang kiểm tra xác thực..."
      text="Loading test authentication page..."
    />
  );
} 